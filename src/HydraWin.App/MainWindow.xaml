<Window x:Class="HydraWin.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:HydraWin.App.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Title="{Binding Title}" Height="600" Width="900"
        MinHeight="300" MinWidth="480">
    <!--
        Task 03 hangs a temporary ItemsControl of tracked windows here; task 07 replaces the
        whole body with the task table, unassigned pane and toolbar.
    -->
    <Grid>
    </Grid>
</Window>
